services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rifa
    ports:
      - "5433:5432"
    volumes:
      - rifa_pg:/var/lib/postgresql/data

volumes:
  rifa_pg:
